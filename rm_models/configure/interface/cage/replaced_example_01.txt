# Using replaced state

# Before state:
# =============
#
# Run on device to capture current settings for this resource:
#    info configure interface cage
# Example output:
#    operational-mode gpon

- name: Replace interface cage configuration with XGS mode
  isam_interface_cage:
    state: replaced
    config:
      - name: "1/1"
        operational_mode: "xgs"

# After state:
# ============
#
# Re-run the same info command to show the effect:
#    info configure interface cage
# Expected output:
#    operational-mode xgs
